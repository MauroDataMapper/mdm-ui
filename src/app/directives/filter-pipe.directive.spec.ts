import { FilterPipe } from './filter-pipe.directive';

describe('FilterPipeDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterPipe();
    expect(directive).toBeTruthy();
  });
});
