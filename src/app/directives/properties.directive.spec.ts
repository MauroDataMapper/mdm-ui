import { PropertiesDirective } from './properties.directive';

describe('PropertiesDirective', () => {
  it('should create an instance', () => {
    const directive = new PropertiesDirective();
    expect(directive).toBeTruthy();
  });
});
